export type User = {
    id: string
    matricula: string
    nome: string
    email: string
    supervisor: string
    coordenador: string
    operacao: string
    site: string
    exp: number
    permissoes: boolean
}
